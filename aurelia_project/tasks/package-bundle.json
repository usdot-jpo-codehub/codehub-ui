{
  "name": "package-bundle",
  "description": "Package the bundle and its assets in a target directory.",
  "flags": [
    {
      "name": "env",
      "description": "Sets the build environment.",
      "type": "string"
    }
  ]
}
